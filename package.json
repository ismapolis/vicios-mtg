{
  "name": "vicios-mtg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "client": "yarn workspace @vicios-mtg/client",
    "common": "yarn workspace @vicios-mtg/common",
    "server": "yarn workspace @vicios-mtg/server",
    "dev": "yarn common build & yarn client dev & yarn server dev --verbose",
    "start": "yarn server start"
  }
}
